import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: { main: '#5bc0be' },
    secondary: { main: '#3a506b' },
    background: { default: '#0b132b' },
    success: { main: '#32936f' },
    warning: { main: '#ffbf00' },
    error: { main: '#e83f6f' },
  },
})
